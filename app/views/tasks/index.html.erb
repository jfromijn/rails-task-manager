<h1>My Tasks</h1>
<% @tasks.each do |task| %>
<!-- <p><%= task.details %> </p>
<p><%= task.completed %> </p> -->
<p><input type="checkbox" disabled <%= "checked" if (task.completed)%>> <%= task.title%> <%= link_to "details", task_path(task) %> <%= link_to 'delete', task_path(task), method: :delete, data: { confirm: "Are you sure? Really?"} %></p>

<% end %>


<%= link_to "add", new_task_path() %>
